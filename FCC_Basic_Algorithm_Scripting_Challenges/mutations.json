{"index.js":"function mutation(arr) {\n// makes both strings in the array lowercase for matching purposes\nvar word0 = arr[0].toLowerCase();\nvar word1 = arr[1].toLowerCase();\n \n// iterates through each letter of the second word\nfor (var i = 0; i< word1.length; i++){ \n\n// value is set to check if there is an index of the first word in the second word\nvar value = word0.indexOf(word1[i]);\n\n// with indexOf, if it shows -1 it means it is negative and therefore there is no presence of the first word in the second\n// therefore we set it to equal false\nif (value === -1){\n\n  return false\n} \n\n}\n// if everything is fine, it will return true\n  return true;\n}\n// in this case it will return false as hello does not containt not contain the letter y\nmutation([\"hello\", \"hey\"]);"}